// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.2
//   protoc               v6.33.5
// source: core/v1/api/product_api.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { ApiError } from "../types/common.js";
import { Product } from "../types/product.js";
import { Variant } from "../types/variant.js";

export const protobufPackage = "darkfactory.core.v1";

/** POST /products */
export interface CreateProductRequest {
  /** Required. Format: @local://{abs_path} */
  productLocator: string;
  displayName?: string | undefined;
}

export interface CreateProductSuccess {
  product:
    | Product
    | undefined;
  /** Stage 0 invariant: creation also creates the default variant. */
  defaultVariant: Variant | undefined;
}

export interface CreateProductResponse {
  ok?: CreateProductSuccess | undefined;
  error?: ApiError | undefined;
}

function createBaseCreateProductRequest(): CreateProductRequest {
  return { productLocator: "", displayName: undefined };
}

export const CreateProductRequest: MessageFns<CreateProductRequest> = {
  encode(message: CreateProductRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.productLocator !== "") {
      writer.uint32(10).string(message.productLocator);
    }
    if (message.displayName !== undefined) {
      writer.uint32(18).string(message.displayName);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CreateProductRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCreateProductRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.productLocator = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.displayName = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CreateProductRequest {
    return {
      productLocator: isSet(object.productLocator)
        ? globalThis.String(object.productLocator)
        : isSet(object.product_locator)
        ? globalThis.String(object.product_locator)
        : "",
      displayName: isSet(object.displayName)
        ? globalThis.String(object.displayName)
        : isSet(object.display_name)
        ? globalThis.String(object.display_name)
        : undefined,
    };
  },

  toJSON(message: CreateProductRequest): unknown {
    const obj: any = {};
    if (message.productLocator !== "") {
      obj.productLocator = message.productLocator;
    }
    if (message.displayName !== undefined) {
      obj.displayName = message.displayName;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CreateProductRequest>, I>>(base?: I): CreateProductRequest {
    return CreateProductRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CreateProductRequest>, I>>(object: I): CreateProductRequest {
    const message = createBaseCreateProductRequest();
    message.productLocator = object.productLocator ?? "";
    message.displayName = object.displayName ?? undefined;
    return message;
  },
};

function createBaseCreateProductSuccess(): CreateProductSuccess {
  return { product: undefined, defaultVariant: undefined };
}

export const CreateProductSuccess: MessageFns<CreateProductSuccess> = {
  encode(message: CreateProductSuccess, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.product !== undefined) {
      Product.encode(message.product, writer.uint32(10).fork()).join();
    }
    if (message.defaultVariant !== undefined) {
      Variant.encode(message.defaultVariant, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CreateProductSuccess {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCreateProductSuccess();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.product = Product.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.defaultVariant = Variant.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CreateProductSuccess {
    return {
      product: isSet(object.product) ? Product.fromJSON(object.product) : undefined,
      defaultVariant: isSet(object.defaultVariant)
        ? Variant.fromJSON(object.defaultVariant)
        : isSet(object.default_variant)
        ? Variant.fromJSON(object.default_variant)
        : undefined,
    };
  },

  toJSON(message: CreateProductSuccess): unknown {
    const obj: any = {};
    if (message.product !== undefined) {
      obj.product = Product.toJSON(message.product);
    }
    if (message.defaultVariant !== undefined) {
      obj.defaultVariant = Variant.toJSON(message.defaultVariant);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CreateProductSuccess>, I>>(base?: I): CreateProductSuccess {
    return CreateProductSuccess.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CreateProductSuccess>, I>>(object: I): CreateProductSuccess {
    const message = createBaseCreateProductSuccess();
    message.product = (object.product !== undefined && object.product !== null)
      ? Product.fromPartial(object.product)
      : undefined;
    message.defaultVariant = (object.defaultVariant !== undefined && object.defaultVariant !== null)
      ? Variant.fromPartial(object.defaultVariant)
      : undefined;
    return message;
  },
};

function createBaseCreateProductResponse(): CreateProductResponse {
  return { ok: undefined, error: undefined };
}

export const CreateProductResponse: MessageFns<CreateProductResponse> = {
  encode(message: CreateProductResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.ok !== undefined) {
      CreateProductSuccess.encode(message.ok, writer.uint32(10).fork()).join();
    }
    if (message.error !== undefined) {
      ApiError.encode(message.error, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CreateProductResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCreateProductResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.ok = CreateProductSuccess.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.error = ApiError.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CreateProductResponse {
    return {
      ok: isSet(object.ok) ? CreateProductSuccess.fromJSON(object.ok) : undefined,
      error: isSet(object.error) ? ApiError.fromJSON(object.error) : undefined,
    };
  },

  toJSON(message: CreateProductResponse): unknown {
    const obj: any = {};
    if (message.ok !== undefined) {
      obj.ok = CreateProductSuccess.toJSON(message.ok);
    }
    if (message.error !== undefined) {
      obj.error = ApiError.toJSON(message.error);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CreateProductResponse>, I>>(base?: I): CreateProductResponse {
    return CreateProductResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CreateProductResponse>, I>>(object: I): CreateProductResponse {
    const message = createBaseCreateProductResponse();
    message.ok = (object.ok !== undefined && object.ok !== null)
      ? CreateProductSuccess.fromPartial(object.ok)
      : undefined;
    message.error = (object.error !== undefined && object.error !== null)
      ? ApiError.fromPartial(object.error)
      : undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
