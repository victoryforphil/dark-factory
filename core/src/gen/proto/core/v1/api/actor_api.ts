// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.2
//   protoc               v6.33.5
// source: core/v1/api/actor_api.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Actor } from "../types/actor.js";
import { ActorKind, actorKindFromJSON, actorKindToJSON, ApiError } from "../types/common.js";

export const protobufPackage = "darkfactory.core.v1";

/** POST /actors/spawn */
export interface SpawnActorRequest {
  productId: string;
  variantId: string;
  /** Stage 0: ACTOR_KIND_OPENCODE_SESSION */
  kind: ActorKind;
  metadata: { [key: string]: string };
}

export interface SpawnActorRequest_MetadataEntry {
  key: string;
  value: string;
}

export interface SpawnActorSuccess {
  actor: Actor | undefined;
}

export interface SpawnActorResponse {
  ok?: SpawnActorSuccess | undefined;
  error?: ApiError | undefined;
}

/** GET /actors/:actor_id/state */
export interface GetActorStateRequest {
  actorId: string;
}

export interface GetActorStateSuccess {
  actor: Actor | undefined;
}

export interface GetActorStateResponse {
  ok?: GetActorStateSuccess | undefined;
  error?: ApiError | undefined;
}

function createBaseSpawnActorRequest(): SpawnActorRequest {
  return { productId: "", variantId: "", kind: 0, metadata: {} };
}

export const SpawnActorRequest: MessageFns<SpawnActorRequest> = {
  encode(message: SpawnActorRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.productId !== "") {
      writer.uint32(10).string(message.productId);
    }
    if (message.variantId !== "") {
      writer.uint32(18).string(message.variantId);
    }
    if (message.kind !== 0) {
      writer.uint32(24).int32(message.kind);
    }
    globalThis.Object.entries(message.metadata).forEach(([key, value]: [string, string]) => {
      SpawnActorRequest_MetadataEntry.encode({ key: key as any, value }, writer.uint32(34).fork()).join();
    });
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SpawnActorRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSpawnActorRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.productId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.variantId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.kind = reader.int32() as any;
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          const entry4 = SpawnActorRequest_MetadataEntry.decode(reader, reader.uint32());
          if (entry4.value !== undefined) {
            message.metadata[entry4.key] = entry4.value;
          }
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SpawnActorRequest {
    return {
      productId: isSet(object.productId)
        ? globalThis.String(object.productId)
        : isSet(object.product_id)
        ? globalThis.String(object.product_id)
        : "",
      variantId: isSet(object.variantId)
        ? globalThis.String(object.variantId)
        : isSet(object.variant_id)
        ? globalThis.String(object.variant_id)
        : "",
      kind: isSet(object.kind) ? actorKindFromJSON(object.kind) : 0,
      metadata: isObject(object.metadata)
        ? (globalThis.Object.entries(object.metadata) as [string, any][]).reduce(
          (acc: { [key: string]: string }, [key, value]: [string, any]) => {
            acc[key] = globalThis.String(value);
            return acc;
          },
          {},
        )
        : {},
    };
  },

  toJSON(message: SpawnActorRequest): unknown {
    const obj: any = {};
    if (message.productId !== "") {
      obj.productId = message.productId;
    }
    if (message.variantId !== "") {
      obj.variantId = message.variantId;
    }
    if (message.kind !== 0) {
      obj.kind = actorKindToJSON(message.kind);
    }
    if (message.metadata) {
      const entries = globalThis.Object.entries(message.metadata) as [string, string][];
      if (entries.length > 0) {
        obj.metadata = {};
        entries.forEach(([k, v]) => {
          obj.metadata[k] = v;
        });
      }
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SpawnActorRequest>, I>>(base?: I): SpawnActorRequest {
    return SpawnActorRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SpawnActorRequest>, I>>(object: I): SpawnActorRequest {
    const message = createBaseSpawnActorRequest();
    message.productId = object.productId ?? "";
    message.variantId = object.variantId ?? "";
    message.kind = object.kind ?? 0;
    message.metadata = (globalThis.Object.entries(object.metadata ?? {}) as [string, string][]).reduce(
      (acc: { [key: string]: string }, [key, value]: [string, string]) => {
        if (value !== undefined) {
          acc[key] = globalThis.String(value);
        }
        return acc;
      },
      {},
    );
    return message;
  },
};

function createBaseSpawnActorRequest_MetadataEntry(): SpawnActorRequest_MetadataEntry {
  return { key: "", value: "" };
}

export const SpawnActorRequest_MetadataEntry: MessageFns<SpawnActorRequest_MetadataEntry> = {
  encode(message: SpawnActorRequest_MetadataEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== "") {
      writer.uint32(18).string(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SpawnActorRequest_MetadataEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSpawnActorRequest_MetadataEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SpawnActorRequest_MetadataEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? globalThis.String(object.value) : "",
    };
  },

  toJSON(message: SpawnActorRequest_MetadataEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== "") {
      obj.value = message.value;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SpawnActorRequest_MetadataEntry>, I>>(base?: I): SpawnActorRequest_MetadataEntry {
    return SpawnActorRequest_MetadataEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SpawnActorRequest_MetadataEntry>, I>>(
    object: I,
  ): SpawnActorRequest_MetadataEntry {
    const message = createBaseSpawnActorRequest_MetadataEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? "";
    return message;
  },
};

function createBaseSpawnActorSuccess(): SpawnActorSuccess {
  return { actor: undefined };
}

export const SpawnActorSuccess: MessageFns<SpawnActorSuccess> = {
  encode(message: SpawnActorSuccess, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.actor !== undefined) {
      Actor.encode(message.actor, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SpawnActorSuccess {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSpawnActorSuccess();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.actor = Actor.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SpawnActorSuccess {
    return { actor: isSet(object.actor) ? Actor.fromJSON(object.actor) : undefined };
  },

  toJSON(message: SpawnActorSuccess): unknown {
    const obj: any = {};
    if (message.actor !== undefined) {
      obj.actor = Actor.toJSON(message.actor);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SpawnActorSuccess>, I>>(base?: I): SpawnActorSuccess {
    return SpawnActorSuccess.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SpawnActorSuccess>, I>>(object: I): SpawnActorSuccess {
    const message = createBaseSpawnActorSuccess();
    message.actor = (object.actor !== undefined && object.actor !== null) ? Actor.fromPartial(object.actor) : undefined;
    return message;
  },
};

function createBaseSpawnActorResponse(): SpawnActorResponse {
  return { ok: undefined, error: undefined };
}

export const SpawnActorResponse: MessageFns<SpawnActorResponse> = {
  encode(message: SpawnActorResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.ok !== undefined) {
      SpawnActorSuccess.encode(message.ok, writer.uint32(10).fork()).join();
    }
    if (message.error !== undefined) {
      ApiError.encode(message.error, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SpawnActorResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSpawnActorResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.ok = SpawnActorSuccess.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.error = ApiError.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SpawnActorResponse {
    return {
      ok: isSet(object.ok) ? SpawnActorSuccess.fromJSON(object.ok) : undefined,
      error: isSet(object.error) ? ApiError.fromJSON(object.error) : undefined,
    };
  },

  toJSON(message: SpawnActorResponse): unknown {
    const obj: any = {};
    if (message.ok !== undefined) {
      obj.ok = SpawnActorSuccess.toJSON(message.ok);
    }
    if (message.error !== undefined) {
      obj.error = ApiError.toJSON(message.error);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SpawnActorResponse>, I>>(base?: I): SpawnActorResponse {
    return SpawnActorResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SpawnActorResponse>, I>>(object: I): SpawnActorResponse {
    const message = createBaseSpawnActorResponse();
    message.ok = (object.ok !== undefined && object.ok !== null) ? SpawnActorSuccess.fromPartial(object.ok) : undefined;
    message.error = (object.error !== undefined && object.error !== null)
      ? ApiError.fromPartial(object.error)
      : undefined;
    return message;
  },
};

function createBaseGetActorStateRequest(): GetActorStateRequest {
  return { actorId: "" };
}

export const GetActorStateRequest: MessageFns<GetActorStateRequest> = {
  encode(message: GetActorStateRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.actorId !== "") {
      writer.uint32(10).string(message.actorId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetActorStateRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetActorStateRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.actorId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetActorStateRequest {
    return {
      actorId: isSet(object.actorId)
        ? globalThis.String(object.actorId)
        : isSet(object.actor_id)
        ? globalThis.String(object.actor_id)
        : "",
    };
  },

  toJSON(message: GetActorStateRequest): unknown {
    const obj: any = {};
    if (message.actorId !== "") {
      obj.actorId = message.actorId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetActorStateRequest>, I>>(base?: I): GetActorStateRequest {
    return GetActorStateRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetActorStateRequest>, I>>(object: I): GetActorStateRequest {
    const message = createBaseGetActorStateRequest();
    message.actorId = object.actorId ?? "";
    return message;
  },
};

function createBaseGetActorStateSuccess(): GetActorStateSuccess {
  return { actor: undefined };
}

export const GetActorStateSuccess: MessageFns<GetActorStateSuccess> = {
  encode(message: GetActorStateSuccess, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.actor !== undefined) {
      Actor.encode(message.actor, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetActorStateSuccess {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetActorStateSuccess();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.actor = Actor.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetActorStateSuccess {
    return { actor: isSet(object.actor) ? Actor.fromJSON(object.actor) : undefined };
  },

  toJSON(message: GetActorStateSuccess): unknown {
    const obj: any = {};
    if (message.actor !== undefined) {
      obj.actor = Actor.toJSON(message.actor);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetActorStateSuccess>, I>>(base?: I): GetActorStateSuccess {
    return GetActorStateSuccess.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetActorStateSuccess>, I>>(object: I): GetActorStateSuccess {
    const message = createBaseGetActorStateSuccess();
    message.actor = (object.actor !== undefined && object.actor !== null) ? Actor.fromPartial(object.actor) : undefined;
    return message;
  },
};

function createBaseGetActorStateResponse(): GetActorStateResponse {
  return { ok: undefined, error: undefined };
}

export const GetActorStateResponse: MessageFns<GetActorStateResponse> = {
  encode(message: GetActorStateResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.ok !== undefined) {
      GetActorStateSuccess.encode(message.ok, writer.uint32(10).fork()).join();
    }
    if (message.error !== undefined) {
      ApiError.encode(message.error, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetActorStateResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetActorStateResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.ok = GetActorStateSuccess.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.error = ApiError.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetActorStateResponse {
    return {
      ok: isSet(object.ok) ? GetActorStateSuccess.fromJSON(object.ok) : undefined,
      error: isSet(object.error) ? ApiError.fromJSON(object.error) : undefined,
    };
  },

  toJSON(message: GetActorStateResponse): unknown {
    const obj: any = {};
    if (message.ok !== undefined) {
      obj.ok = GetActorStateSuccess.toJSON(message.ok);
    }
    if (message.error !== undefined) {
      obj.error = ApiError.toJSON(message.error);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetActorStateResponse>, I>>(base?: I): GetActorStateResponse {
    return GetActorStateResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetActorStateResponse>, I>>(object: I): GetActorStateResponse {
    const message = createBaseGetActorStateResponse();
    message.ok = (object.ok !== undefined && object.ok !== null)
      ? GetActorStateSuccess.fromPartial(object.ok)
      : undefined;
    message.error = (object.error !== undefined && object.error !== null)
      ? ApiError.fromPartial(object.error)
      : undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isObject(value: any): boolean {
  return typeof value === "object" && value !== null;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
