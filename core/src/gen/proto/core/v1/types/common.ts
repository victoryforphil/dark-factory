// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.2
//   protoc               v6.33.5
// source: core/v1/types/common.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "darkfactory.core.v1";

export enum ActorKind {
  ACTOR_KIND_UNSPECIFIED = 0,
  ACTOR_KIND_OPENCODE_SESSION = 1,
  UNRECOGNIZED = -1,
}

export function actorKindFromJSON(object: any): ActorKind {
  switch (object) {
    case 0:
    case "ACTOR_KIND_UNSPECIFIED":
      return ActorKind.ACTOR_KIND_UNSPECIFIED;
    case 1:
    case "ACTOR_KIND_OPENCODE_SESSION":
      return ActorKind.ACTOR_KIND_OPENCODE_SESSION;
    case -1:
    case "UNRECOGNIZED":
    default:
      return ActorKind.UNRECOGNIZED;
  }
}

export function actorKindToJSON(object: ActorKind): string {
  switch (object) {
    case ActorKind.ACTOR_KIND_UNSPECIFIED:
      return "ACTOR_KIND_UNSPECIFIED";
    case ActorKind.ACTOR_KIND_OPENCODE_SESSION:
      return "ACTOR_KIND_OPENCODE_SESSION";
    case ActorKind.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export enum ActorStatus {
  ACTOR_STATUS_UNSPECIFIED = 0,
  ACTOR_STATUS_STARTING = 1,
  ACTOR_STATUS_RUNNING = 2,
  ACTOR_STATUS_STOPPED = 3,
  ACTOR_STATUS_FAILED = 4,
  UNRECOGNIZED = -1,
}

export function actorStatusFromJSON(object: any): ActorStatus {
  switch (object) {
    case 0:
    case "ACTOR_STATUS_UNSPECIFIED":
      return ActorStatus.ACTOR_STATUS_UNSPECIFIED;
    case 1:
    case "ACTOR_STATUS_STARTING":
      return ActorStatus.ACTOR_STATUS_STARTING;
    case 2:
    case "ACTOR_STATUS_RUNNING":
      return ActorStatus.ACTOR_STATUS_RUNNING;
    case 3:
    case "ACTOR_STATUS_STOPPED":
      return ActorStatus.ACTOR_STATUS_STOPPED;
    case 4:
    case "ACTOR_STATUS_FAILED":
      return ActorStatus.ACTOR_STATUS_FAILED;
    case -1:
    case "UNRECOGNIZED":
    default:
      return ActorStatus.UNRECOGNIZED;
  }
}

export function actorStatusToJSON(object: ActorStatus): string {
  switch (object) {
    case ActorStatus.ACTOR_STATUS_UNSPECIFIED:
      return "ACTOR_STATUS_UNSPECIFIED";
    case ActorStatus.ACTOR_STATUS_STARTING:
      return "ACTOR_STATUS_STARTING";
    case ActorStatus.ACTOR_STATUS_RUNNING:
      return "ACTOR_STATUS_RUNNING";
    case ActorStatus.ACTOR_STATUS_STOPPED:
      return "ACTOR_STATUS_STOPPED";
    case ActorStatus.ACTOR_STATUS_FAILED:
      return "ACTOR_STATUS_FAILED";
    case ActorStatus.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export interface ApiError {
  code: string;
  message: string;
  details: { [key: string]: string };
}

export interface ApiError_DetailsEntry {
  key: string;
  value: string;
}

function createBaseApiError(): ApiError {
  return { code: "", message: "", details: {} };
}

export const ApiError: MessageFns<ApiError> = {
  encode(message: ApiError, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.code !== "") {
      writer.uint32(10).string(message.code);
    }
    if (message.message !== "") {
      writer.uint32(18).string(message.message);
    }
    globalThis.Object.entries(message.details).forEach(([key, value]: [string, string]) => {
      ApiError_DetailsEntry.encode({ key: key as any, value }, writer.uint32(26).fork()).join();
    });
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ApiError {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseApiError();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.code = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.message = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          const entry3 = ApiError_DetailsEntry.decode(reader, reader.uint32());
          if (entry3.value !== undefined) {
            message.details[entry3.key] = entry3.value;
          }
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ApiError {
    return {
      code: isSet(object.code) ? globalThis.String(object.code) : "",
      message: isSet(object.message) ? globalThis.String(object.message) : "",
      details: isObject(object.details)
        ? (globalThis.Object.entries(object.details) as [string, any][]).reduce(
          (acc: { [key: string]: string }, [key, value]: [string, any]) => {
            acc[key] = globalThis.String(value);
            return acc;
          },
          {},
        )
        : {},
    };
  },

  toJSON(message: ApiError): unknown {
    const obj: any = {};
    if (message.code !== "") {
      obj.code = message.code;
    }
    if (message.message !== "") {
      obj.message = message.message;
    }
    if (message.details) {
      const entries = globalThis.Object.entries(message.details) as [string, string][];
      if (entries.length > 0) {
        obj.details = {};
        entries.forEach(([k, v]) => {
          obj.details[k] = v;
        });
      }
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ApiError>, I>>(base?: I): ApiError {
    return ApiError.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ApiError>, I>>(object: I): ApiError {
    const message = createBaseApiError();
    message.code = object.code ?? "";
    message.message = object.message ?? "";
    message.details = (globalThis.Object.entries(object.details ?? {}) as [string, string][]).reduce(
      (acc: { [key: string]: string }, [key, value]: [string, string]) => {
        if (value !== undefined) {
          acc[key] = globalThis.String(value);
        }
        return acc;
      },
      {},
    );
    return message;
  },
};

function createBaseApiError_DetailsEntry(): ApiError_DetailsEntry {
  return { key: "", value: "" };
}

export const ApiError_DetailsEntry: MessageFns<ApiError_DetailsEntry> = {
  encode(message: ApiError_DetailsEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== "") {
      writer.uint32(18).string(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ApiError_DetailsEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseApiError_DetailsEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ApiError_DetailsEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? globalThis.String(object.value) : "",
    };
  },

  toJSON(message: ApiError_DetailsEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== "") {
      obj.value = message.value;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ApiError_DetailsEntry>, I>>(base?: I): ApiError_DetailsEntry {
    return ApiError_DetailsEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ApiError_DetailsEntry>, I>>(object: I): ApiError_DetailsEntry {
    const message = createBaseApiError_DetailsEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? "";
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isObject(value: any): boolean {
  return typeof value === "object" && value !== null;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
