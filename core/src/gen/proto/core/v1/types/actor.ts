// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.2
//   protoc               v6.33.5
// source: core/v1/types/actor.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Timestamp } from "../../../google/protobuf/timestamp.js";
import {
  ActorKind,
  actorKindFromJSON,
  actorKindToJSON,
  ActorStatus,
  actorStatusFromJSON,
  actorStatusToJSON,
} from "./common.js";

export const protobufPackage = "darkfactory.core.v1";

export interface Actor {
  id: string;
  productId: string;
  variantId: string;
  kind: ActorKind;
  status: ActorStatus;
  endpoint: string;
  transport: string;
  pid: number;
  spawnedAt: Date | undefined;
  lastHeartbeatAt: Date | undefined;
  stoppedAt: Date | undefined;
  lastError: string;
  metadata: { [key: string]: string };
}

export interface Actor_MetadataEntry {
  key: string;
  value: string;
}

function createBaseActor(): Actor {
  return {
    id: "",
    productId: "",
    variantId: "",
    kind: 0,
    status: 0,
    endpoint: "",
    transport: "",
    pid: 0,
    spawnedAt: undefined,
    lastHeartbeatAt: undefined,
    stoppedAt: undefined,
    lastError: "",
    metadata: {},
  };
}

export const Actor: MessageFns<Actor> = {
  encode(message: Actor, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.productId !== "") {
      writer.uint32(18).string(message.productId);
    }
    if (message.variantId !== "") {
      writer.uint32(26).string(message.variantId);
    }
    if (message.kind !== 0) {
      writer.uint32(32).int32(message.kind);
    }
    if (message.status !== 0) {
      writer.uint32(40).int32(message.status);
    }
    if (message.endpoint !== "") {
      writer.uint32(50).string(message.endpoint);
    }
    if (message.transport !== "") {
      writer.uint32(58).string(message.transport);
    }
    if (message.pid !== 0) {
      writer.uint32(64).int64(message.pid);
    }
    if (message.spawnedAt !== undefined) {
      Timestamp.encode(toTimestamp(message.spawnedAt), writer.uint32(74).fork()).join();
    }
    if (message.lastHeartbeatAt !== undefined) {
      Timestamp.encode(toTimestamp(message.lastHeartbeatAt), writer.uint32(82).fork()).join();
    }
    if (message.stoppedAt !== undefined) {
      Timestamp.encode(toTimestamp(message.stoppedAt), writer.uint32(90).fork()).join();
    }
    if (message.lastError !== "") {
      writer.uint32(98).string(message.lastError);
    }
    globalThis.Object.entries(message.metadata).forEach(([key, value]: [string, string]) => {
      Actor_MetadataEntry.encode({ key: key as any, value }, writer.uint32(106).fork()).join();
    });
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Actor {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseActor();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.productId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.variantId = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.kind = reader.int32() as any;
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.status = reader.int32() as any;
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.endpoint = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.transport = reader.string();
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.pid = longToNumber(reader.int64());
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.spawnedAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.lastHeartbeatAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 11: {
          if (tag !== 90) {
            break;
          }

          message.stoppedAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 12: {
          if (tag !== 98) {
            break;
          }

          message.lastError = reader.string();
          continue;
        }
        case 13: {
          if (tag !== 106) {
            break;
          }

          const entry13 = Actor_MetadataEntry.decode(reader, reader.uint32());
          if (entry13.value !== undefined) {
            message.metadata[entry13.key] = entry13.value;
          }
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Actor {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      productId: isSet(object.productId)
        ? globalThis.String(object.productId)
        : isSet(object.product_id)
        ? globalThis.String(object.product_id)
        : "",
      variantId: isSet(object.variantId)
        ? globalThis.String(object.variantId)
        : isSet(object.variant_id)
        ? globalThis.String(object.variant_id)
        : "",
      kind: isSet(object.kind) ? actorKindFromJSON(object.kind) : 0,
      status: isSet(object.status) ? actorStatusFromJSON(object.status) : 0,
      endpoint: isSet(object.endpoint) ? globalThis.String(object.endpoint) : "",
      transport: isSet(object.transport) ? globalThis.String(object.transport) : "",
      pid: isSet(object.pid) ? globalThis.Number(object.pid) : 0,
      spawnedAt: isSet(object.spawnedAt)
        ? fromJsonTimestamp(object.spawnedAt)
        : isSet(object.spawned_at)
        ? fromJsonTimestamp(object.spawned_at)
        : undefined,
      lastHeartbeatAt: isSet(object.lastHeartbeatAt)
        ? fromJsonTimestamp(object.lastHeartbeatAt)
        : isSet(object.last_heartbeat_at)
        ? fromJsonTimestamp(object.last_heartbeat_at)
        : undefined,
      stoppedAt: isSet(object.stoppedAt)
        ? fromJsonTimestamp(object.stoppedAt)
        : isSet(object.stopped_at)
        ? fromJsonTimestamp(object.stopped_at)
        : undefined,
      lastError: isSet(object.lastError)
        ? globalThis.String(object.lastError)
        : isSet(object.last_error)
        ? globalThis.String(object.last_error)
        : "",
      metadata: isObject(object.metadata)
        ? (globalThis.Object.entries(object.metadata) as [string, any][]).reduce(
          (acc: { [key: string]: string }, [key, value]: [string, any]) => {
            acc[key] = globalThis.String(value);
            return acc;
          },
          {},
        )
        : {},
    };
  },

  toJSON(message: Actor): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.productId !== "") {
      obj.productId = message.productId;
    }
    if (message.variantId !== "") {
      obj.variantId = message.variantId;
    }
    if (message.kind !== 0) {
      obj.kind = actorKindToJSON(message.kind);
    }
    if (message.status !== 0) {
      obj.status = actorStatusToJSON(message.status);
    }
    if (message.endpoint !== "") {
      obj.endpoint = message.endpoint;
    }
    if (message.transport !== "") {
      obj.transport = message.transport;
    }
    if (message.pid !== 0) {
      obj.pid = Math.round(message.pid);
    }
    if (message.spawnedAt !== undefined) {
      obj.spawnedAt = message.spawnedAt.toISOString();
    }
    if (message.lastHeartbeatAt !== undefined) {
      obj.lastHeartbeatAt = message.lastHeartbeatAt.toISOString();
    }
    if (message.stoppedAt !== undefined) {
      obj.stoppedAt = message.stoppedAt.toISOString();
    }
    if (message.lastError !== "") {
      obj.lastError = message.lastError;
    }
    if (message.metadata) {
      const entries = globalThis.Object.entries(message.metadata) as [string, string][];
      if (entries.length > 0) {
        obj.metadata = {};
        entries.forEach(([k, v]) => {
          obj.metadata[k] = v;
        });
      }
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Actor>, I>>(base?: I): Actor {
    return Actor.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Actor>, I>>(object: I): Actor {
    const message = createBaseActor();
    message.id = object.id ?? "";
    message.productId = object.productId ?? "";
    message.variantId = object.variantId ?? "";
    message.kind = object.kind ?? 0;
    message.status = object.status ?? 0;
    message.endpoint = object.endpoint ?? "";
    message.transport = object.transport ?? "";
    message.pid = object.pid ?? 0;
    message.spawnedAt = object.spawnedAt ?? undefined;
    message.lastHeartbeatAt = object.lastHeartbeatAt ?? undefined;
    message.stoppedAt = object.stoppedAt ?? undefined;
    message.lastError = object.lastError ?? "";
    message.metadata = (globalThis.Object.entries(object.metadata ?? {}) as [string, string][]).reduce(
      (acc: { [key: string]: string }, [key, value]: [string, string]) => {
        if (value !== undefined) {
          acc[key] = globalThis.String(value);
        }
        return acc;
      },
      {},
    );
    return message;
  },
};

function createBaseActor_MetadataEntry(): Actor_MetadataEntry {
  return { key: "", value: "" };
}

export const Actor_MetadataEntry: MessageFns<Actor_MetadataEntry> = {
  encode(message: Actor_MetadataEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== "") {
      writer.uint32(18).string(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Actor_MetadataEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseActor_MetadataEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Actor_MetadataEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? globalThis.String(object.value) : "",
    };
  },

  toJSON(message: Actor_MetadataEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== "") {
      obj.value = message.value;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Actor_MetadataEntry>, I>>(base?: I): Actor_MetadataEntry {
    return Actor_MetadataEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Actor_MetadataEntry>, I>>(object: I): Actor_MetadataEntry {
    const message = createBaseActor_MetadataEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? "";
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function toTimestamp(date: Date): Timestamp {
  const seconds = Math.trunc(date.getTime() / 1_000);
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (t.seconds || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

function isObject(value: any): boolean {
  return typeof value === "object" && value !== null;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
